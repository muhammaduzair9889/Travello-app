# Core Django & REST Framework
Django==4.2.7
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1

# Real-time & WebSocket Support
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.0

# HTTP & Network
requests==2.31.0
urllib3==2.1.0

# Configuration & Environment
python-decouple==3.8
python-dotenv==1.0.0

# Authentication & Security
google-auth==2.29.0
cryptography==41.0.7

# Server & Deployment
gunicorn==21.2.0
whitenoise==6.6.0

# Database
dj-database-url==2.1.0
psycopg2-binary==2.9.9

# Payments & External Services
stripe==8.1.0
sentry-sdk[django]==1.39.2
cloudinary==1.36.0

# AI/ML packages — for AI Itinerary Planner & Recommendation Engine
transformers==4.35.2
torch==2.2.2
scikit-learn==1.3.2
pandas==2.1.4
numpy==1.26.2
scipy==1.11.4
sentencepiece==0.2.0
sentence-transformers==2.2.2
tqdm==4.66.1

# ML-based Ranking (Optional but recommended for itinerary AI)
# Graceful fallback to rule-based ranking if not installed
lightgbm==4.1.0

# Vector Search & Embeddings (ML System)
faiss-cpu==1.7.4

# Web Scraping (Booking.com hotel data)
# Graceful fallback if not installed - uses Puppeteer.js instead
selenium==4.15.2

# Sentiment Analysis & NLP (Review system)
# Graceful fallback to keyword-based sentiment if not installed
textblob==0.17.1




